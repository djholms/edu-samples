#create cluster

# Step 0
# create Jenkins

kubectl create ns jenkins
kubectl create -f jenkins-deployment.yaml
kubectl get deployment
kubectl  describe deployments

#! Run service as load balancer
kubectl expose deployment jenkins-deployment --type=LoadBalancer
#kubectl create -f jenkins-service.yaml --type=LoadBalancer


# Step 1
# create secret 
kubectl create secret generic jenkins-admin-pass --from-file=./taks1/jenkins_secret.txt

# Create configmap
kubectl create configmap jenkins-pluggins --from-file=./taks1/pluggins.txt 
kubectl get configmaps
kubectl describe configmaps jenkins-pluggins

